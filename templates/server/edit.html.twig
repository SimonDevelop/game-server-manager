{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit a server'|trans }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>{{ 'Edit a server'|trans }}</h1>

    {{ include('server/_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('server_index') }}">{{ 'Back to the list'|trans }}</a>
</div>
{% endblock %}
